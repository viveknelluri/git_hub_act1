name:hello_world
on :
 push:
  branch:
   -main
 pull_request:
  branch:
   -main
 workflow_dispatch:

jobs:
  hello:
   runs-on: ubuntu-latest
   steps:
    -uses:actions/checkout@v4
    -name: hello wolrd
     run:echo "Hello"
     shell:bash
